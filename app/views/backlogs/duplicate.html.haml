- title 'Duplicate a backlog'

- content_for :title do
  %h1 Duplicate backlog #{@backlog.name}

= form_for(@new_backlog, :url => duplicate_account_backlog_path(current_account, @backlog)) do |f|
  - unless @new_backlog.errors.blank?
    .form_errors
      The form was not completed correctly.  Please correct the following fields to register:
      %ul
        - @new_backlog.errors.full_messages.each do |error|
          %li
            = error

  %p Please enter a new name for the duplicated backlog

  = f.label :name, raw("New backlog name <span class='detail'>(Old backlog name: #{@backlog.name})</span>")
  %br/
  = f.text_field :name
  %br/
  %br/
  = f.submit "Duplicate backlog"
  = link_to "cancel", account_path(current_account)