- title 'Create a new account'

- content_for :title do
  %h1 Create a new account

%p.notice Do not create a new account if you believe one already exists for your organisation.  To be added to an existing account, contact the account owner who will invite you into their account.

= form_for(@company) do |f|
  - unless @company.errors.blank?
    .form_errors
      The form was not completed correctly.  Please correct the following fields to register:
      %ul
        - @company.errors.full_messages.each do |error|
          %li
            = error
  != render :partial => 'form', :locals => { :f => f }

  %br/
  %br/
  = f.submit "Create new account"
