- title "Access to #{@invite_account.name}"

- content_for :title do
  %h1= "Access to “#{truncate(@invite_account.name, :length => 40)}”"

%p
  = "You have just been granted access to the account “#{truncate(@invite_account.name, :length => 40)}”."

= link_to "Go to “#{truncate(@invite_account.name, :length => 40)}” account", account_path(@invite_account), :class => 'button'