# encoding: utf-8
!= render :partial => '/layouts/head'

!= render :partial => '/layouts/browser_support'

%header
  != render :partial => '/layouts/header'

!= render :partial => '/layouts/alert_space'

%section.content
  %section.title
    = yield(:title)
  - if content_for?(:side_panel)
    %section.side-panel
      = yield(:side_panel)
  %section.main-content-pod
    = yield

!= render :partial => '/layouts/footer'
